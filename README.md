# PWA Demo

## Kör instruktioner

I en terminal kör ```npm run api```i en andra, t ex. terminalen i Visual Studio Code kör ```npm run dev```.
Dev miljön startar själva Vite applikationen och api startar en enkel json server som läser från json filen.
